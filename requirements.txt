# Offline development / analysis dependencies.
# The runtime agent (submission/) uses only the Python standard library so it
# runs inside the engine sandbox without extra installs.
pandas>=2.0
numpy>=1.24
